#posts
  .row
    .span12#jumbotron
      %h1
        Cooking tips
      %span#subheader
        Get better at cooking.
  .row      
    .span10#posts_grid
      - if @posts.count == 0
        .alert.alert-info Sorry, no results match your criteria.
      - else
        - @posts.each do |post|
          %a.span3.thumbnail.post{:href => post_path(post)}
            %img{:src => post.thumbnail}
            %h4=post.title
            %h5=" by " + post.author + " | " + post.created_at.strftime("%b %d, %Y")
        = paginate @posts
        